<% @blog_post.comments.each do |comment| %>
  <h4><%= link_to comment.user.name, user_profile_path(id: comment.user.id) %></h4>
  <p><%= time_ago_in_words(@blog_post.created_at) %></p>
  <p><%= comment.comment_entry %></p>
<% end %>
